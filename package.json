{
    "private": true,
    "scripts": {
        "start": "parcel src/index.html",
        "build": "rm -rf dist/* && parcel build src/index.html"
    },
    "dependencies": {
        "preact": "^10.4.8"
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.4",
        "sass": "^1.26.10"
    }
}
